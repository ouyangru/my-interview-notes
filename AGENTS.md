# Repository instructions

## 面试复盘入库

当新增、重命名或删除 `docs/interviews/` 下的真实面试复盘时，必须把网页入口一起维护完整：

1. 更新 `docs/interviews/index.md` 的“最近复盘”列表。
2. 左侧“面试复盘”导航由 `hooks/interview_nav.py` 在 MkDocs 构建时自动扫描 `docs/interviews/*.md` 生成，不再手工逐条维护 `mkdocs.yml`。
3. 执行 `python scripts/check-interview-nav.py`，确认所有 `docs/interviews/*.md`（`index.md` 除外）都能进入自动生成导航，并且同时存在于复盘首页。
4. 只有复盘文件、复盘首页和自动生成导航三者一致，才算“面试复盘入库”完成；仅提交 Markdown 文件不算完成。

## 知识库双链与小标题

知识页之间、面试复盘到知识页之间建立关联时，优先链接到**具体小标题**，不要只链接到整个大页面。

尤其是原始 fork 题库中的巨型 Markdown（`操作系统.md`、`计算机网络.md`、`C++.md`、`linux服务器.md` 等），除导航/索引页外，禁止把一个具体知识点只链接到文件顶部。必须落到对应章节。

推荐直接使用 Obsidian 风格双链：

- `[[进程与线程]]`：跳到个人知识页顶部；
- `[[进程与线程#线程和进程到底共享什么]]`：跳到个人知识页具体小标题；
- `[[操作系统#页表与 TLB]]`：语义链接到原始 `操作系统.md` 中真正讲页表/TLB 的章节；
- `[[计算机网络#TCP 三次握手]]`：链接到原始网络题库的三次握手章节；
- `[[C++#智能指针]]`：链接到原始 C++ 题库的智能指针章节；
- `[[C++#智能指针|智能指针原始题库]]`：自定义显示文本。

MkDocs 构建时由 `hooks/wikilinks.py` 先解析目标文件，再核对目标小标题真实存在，最后生成 fragment。原始题库的部分标题带 `☆`、`c++` 前缀或使用更宽泛的章节名，因此允许在 `HEADING_ALIASES` 中维护少量稳定的语义别名；不要直接把用户写的概念名机械 slugify 成一个不存在的 fragment。

中文标题使用稳定 Unicode 锚点，不要再手工引用 `#_8`、`#_13` 这类构建序号。如果目标小标题不存在或无法唯一确定，应该保留未解析双链并修正映射，而不是静默生成错误链接。

如果使用普通 Markdown 链接，也必须在有明确对应知识点时带 fragment，例如：`../../操作系统.md#解决速度问题`。新增个人知识页时，优先在“来源 / 原始题库关联”中给出 1～3 个最相关的原始章节深链。

## 线上验证

涉及网页展示的改动，不要把“GitHub commit 已存在”当作“网页已更新”。如果任务要求确认线上结果，还需要检查部署后的实际页面或构建状态。

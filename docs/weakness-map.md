# 薄弱点地图

用于记录重复出错或理解不稳定的知识点。具体知识正文放在 `docs/knowledge/`，本页只保留状态、典型错误、最近表现、出现次数和训练优先级。

> 最近更新：2026-09-18

| 知识点 | 当前状态 | 典型错误 | 最近表现 | 本轮出现次数 | 对应知识页 | 训练优先级 |
|---|---|---|---|---:|---|---|
| 组合计数：等价分段与独立间隙 | [weak] | 没先把任意次相邻合并转化为连续分段；把无零间隙误认为只有一种；单对结论不能直接推广到多个 1 | 讲解后能继续提出连续三个 1 与混合零间隙的边界问题，但尚未独立复述乘积公式或写代码 | 1 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 算法模式识别：从暴力抽象经典结构 | `[weak]` | 过早选堆/DFS/入度等实现，没有先看数据范围、暴力复杂度和重复工作 | 在护栏、驿站、鲤鱼离岛、区间上下界、基站干扰、四件礼物、欧拉路径等题中都需要提示后才能切到标准模型；提示后通常能理解 | 7 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 滑动窗口：合法区间计数与单调性 | `[weak]` | 每个 `left` 都把 `right` 重置到末尾并复制两个堆和频次表，导致接近 `O(n^2)`；固定左端点时错误使用 `len*(len+1)/2` 重复计数；没有先判断负数会破坏 `sum<=S` 的单调性 | 经提示后已经理解“right/left 都只右移”和 `ans += right-left+1`，也能理解负数场景要改用前缀和 + 有序统计结构，但尚未独立写出换皮题 | 1 | [滑动窗口与区间单调性](knowledge/算法/滑动窗口与区间单调性.md) | P0 |
| 区间操作：寻找边界/差分不变量 | `[weak] [written-test]` | 看到区间整体反色后，直接维护原始 0/1 连续段及奇偶长度，没有先分析单次操作对目标函数真正影响的位置 | vivo 笔试中未能现场抽象为边界数组；考后经提示后理解“区间内部相邻关系不变，只翻两个端点”，并能用 `111` 等例子验证 | 1 | [区间操作与边界不变量](knowledge/算法/区间操作与边界不变量.md) | P0 |
| 状态设计：是否缺少影响未来的信息 | `[weak]` | 用 `dp[node]` 压掉电量等关键资源；变量含义中途变化；递归状态污染 | 驿站补给中单一 `(battery,time)` 状态错误；匀差脉冲 `count` 语义不稳定；麻将 DFS 忘记回溯恢复 | 3 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 单调栈：左右最近更大/更小 | `[weak]` | 一开始只会逐点左右暴力扫描；写栈时哨兵、清栈、左右栈变量混淆 | P5315 已能主动想到单调栈；P5413 仍先想到暴力，需要继续换皮回测 | 2 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 图算法边界：BFS / 拓扑 / Dijkstra / 欧拉 | `[weak]` | 看到图容易套最近学过的算法；不知道“每条边恰好一次”对应欧拉；Dijkstra 模板容易忘 | 已能理解 BFS=按层、Dijkstra=按累计代价、Hierholzer=后序收边，但还未独立回测 | 4 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 二分答案 + 差分 | `[weak]` | 最大化最小值时先模拟每次操作；没有先构造 `check(X)` | P5292 经讲解后能理解“最大化最小值 → 二分答案”，尚未独立写出换皮题 | 1 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P1 |
| C++ STL 与实现稳定性 | `[weak] [written-test]` | `sort(end,begin)`、vector 层级混淆、栈空条件写反、字符下标错误、递归不恢复、容器复制时机错误；本次又出现 `for(j...;...;i++)` 死循环与边界判断顺序错误 | 树苗矩形题的整体方向可行，但实现 bug 直接导致 TLE；说明“算法会但代码不稳”仍是重复薄弱点 | 9+ | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P0 |
| 字符串/数值/进制转换 | `[learning] [written-test]` | 对 `stoi`、`to_string`、`stringstream`、`hex/uppercase/setw/setfill` 不熟；IPv4 转十六进制现场约 75% 通过但源代码丢失，无法定位隐藏边界 | 手写十进制解析和 `/16`、`%16` 拆十六进制位的原理正确，现阶段主要缺标准库 API 熟练度与失败用例定位能力 | 1 | [字符串、数值与进制转换](knowledge/C++/字符串数值与进制转换.md) | P1 |
| 0/1 背包与固定件数组合 | `[learning]` | 忘记 `dp[k][sum]`；固定选四件先想价格贪心 + DFS | 能主动提出“四件拆成两个二元组”，说明开始从组合结构降维，但背包转移仍需回测 | 1 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P1 |
| 加权中位数 | `[learning]` | 已找到最优点后疑惑为什么还需要再遍历求目标值 | 已能独立想到 x/y 分离、权重合并和累计权重过半，主要剩实现细节 | 1 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P2 |
| 滚动 DP | `[learning]` | 状态思路正确，但一维/二维访问、vector 赋值细节出错 | 能写出 `pre_dp/dp` 转移，需继续训练原地一维滚动和边界 | 1 | [算法题模式识别与状态设计](knowledge/算法/算法题模式识别与状态设计.md) | P2 |

## 重复薄弱点判断

### 1. 模式识别不是单题失误

本轮至少 7 个题目重复出现“先写复杂实现，后面才发现模型不对”的现象，因此按重复薄弱点处理，而不是单题偶发错误。

训练要求：每题写代码前先口述：

1. 暴力是什么；
2. 暴力复杂度；
3. 重复工作；
4. 经典触发器；
5. 状态定义。

### 2. 实现稳定性不是单纯粗心

`sort` 区间、下标、vector 复制、栈清空、递归恢复等错误跨多题重复出现，需要单独统计。后续训练中应把错误分成两类：

- 模型错误：算法/状态本身不成立；
- 实现错误：模型成立，但 C++ 代码不可靠。

### 3. 当前不能因为“看懂答案”升级状态

这轮多个算法已经能在讲解后理解，但还没有经过新的换皮题独立识别，因此滑动窗口、单调栈、Dijkstra、Hierholzer、二分答案等仍保持 `[weak]`；只有在后续独立完成后再升级为 `[learning]` / `[stable]`。

## 下一轮训练配比

- 60%：`[weak]` / 重复错误——模式识别、滑动窗口单调性、区间操作不变量、单调栈、图算法边界、状态扩图、实现稳定性。
- 25%：`[learning]`——背包/pair 拆分、滚动 DP、加权中位数。
- 15%：稳定回测——BFS、普通 Dijkstra 基础模板、基础回溯。